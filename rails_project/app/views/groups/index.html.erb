<p id="notice"><%= notice %></p>

<h1>Groups</h1>
<%= render 'form' %>
  
<div class="row">
  <div class="col-lg-6">
    <h2>Your Groups</h2>

<table class="table table-hover">
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @groups.each do |group_data| %>
      <tr>
        <td><%= link_to group_data.group_name %></td>
        <td><%= link_to 'Delete',group_path(group_data.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td><%= link_to 'Add',group_path(group_data.id) %></td>


        
         </tr>
    <% end %>
  </tbody>
</table>
</div>
 


  
 


    <% if @groups_member !=nil%>
   
    <% if @group_data !=nil%>
     <h2><%= @group_data.group_name %>Members</h2>
   
<div class="col-lg-6" style="border: 1px solid gray;"> 
   <%= render 'groups_member/form' %>

    <%else%>
     <h2>Data</h2>
  <% end %>

    
    <% @groups_member.each do |group_data| %>
    <div class="col-lg-3"> 
      
       <%= image_tag(group_data.user.picture.url(:thumb)) %>
        <%=  group_data.user.username %>
     <%= link_to 'Remove' ,groups_member_path(group_data), method: :delete, data: { confirm: 'Are you sure?' } %>
</div>
    
    <% end %>
   
 
     <% end %>



</div>



